# 生物多样性曲线优化问题实验报告

## 1. 项目框架概览

本项目围绕 EarthBench / CONOPLib 生物多样性曲线优化问题展开。问题的核心是：给所有物种的首现事件 FAD 和末现事件 LAD 排序，使该排序与地层剖面中的化石观测数据尽可能一致。解是一个长度为 `dims` 的排列，元素为 `0..dims-1`，其中偶数表示某个 taxon 的 FAD，奇数表示对应 LAD。评价函数由 CONOPLib 动态库计算 mismatch penalty，代码中取负数作为 fitness，因此 `y_best` 越接近 0 越好。

项目主要结构如下：

- `configs/`：Hydra 配置，包含任务配置和算法配置。
- `scripts/`：运行脚本，例如 `scripts/run_earth_124.sh`。
- `benchmarks/`：问题定义与黑盒评估，`benchmarks/CONOPLib_*/call.py` 调用 `CONOPLib.so`。
- `algorithms/`：优化算法实现，包括 EA、MAP-Elites、BO、RL 相关算法等。
- `Environments/`：强化学习算子训练/推理使用的环境。
- `main.py`：主训练入口，负责创建任务、实例化算法、Ray 并行评估、记录日志和保存结果。

当前实验主要沿 `scripts/run_earth_124.sh -> main.py -> algorithms/_map_elites.py -> algorithms/_ea_operator.py -> benchmarks/CONOPLib_124/call.py` 这条路径进行。每轮中，算法先通过 `ask()` 生成候选解，再由 `main.py` 中的 Ray worker 调用 benchmark 评估，最后通过 `tell()` 把 fitness 回传给算法更新 archive。

评估过程中，`EarthBenchmark.__call__()` 会先对候选解执行 `repair()`，修复 FAD/LAD 和 coexistence 约束，然后调用 `CONOPLib.so` 计算 penalty。为了保证保存的最优解合法，我们已将主流程改为保存 benchmark 返回的修复后解。

## 2. 交叉与变异算子

项目中的排列算子主要位于 `algorithms/_ea_operator.py`。

### 2.1 Mutation 算子

`swap_mutation`：随机选择两个位置交换。扰动较小，适合局部微调，但探索能力有限。

`insert_mutation`：随机选择一个元素，将其插入到另一个位置。该操作适合事件排序问题，因为它直接改变某个事件在整体年代序列中的位置，同时较好保留其余事件的相对顺序。

`reversal_mutation`：随机选择一段区间并反转。扰动较强，适合跳出局部最优，但可能较大幅度破坏已有结构。

`shuffle_mutation`：随机选择若干元素并打乱。探索性较强，适合增加多样性，但也可能引入较多无效扰动。

`shift_mutation`：在局部邻域内移动或交换某个元素。扰动范围较小，偏向局部搜索。

### 2.2 Crossover 算子

`order_crossover`：保留父代 1 的一个片段，其余位置按父代 2 中的相对顺序补齐。该方法强调保留相对顺序，适合排列优化。

`pmx_crossover`：部分映射交叉，保留父代 1 的片段，并用映射关系处理重复冲突。该方法能同时保留部分位置结构和片段结构。

`cycle_crossover`：根据两个父代之间的位置-元素循环进行继承，交替使用父代片段。该方法能保留父代中的位置关系，但有时变化幅度偏保守。

在 `earth_124` 实验中，baseline 使用 `pmx_crossover + insert_mutation`。从问题性质看，`insert` 对事件排序较自然，因此它本身就是很强的基础变异算子。

## 3. 任务一：不同演化算子的比较设计

作业任务一要求比较不同演化算子对优化效果的影响。由于本问题的解是一个长度为 `dims` 的排列，算子的核心作用是改变事件的相对顺序。我们计划比较框架中已有的 5 个 mutation 算子和 3 个 crossover 算子，并在其他设置尽量一致的情况下观察 `y_best` 收敛曲线、最终最优值和评价次数。

### 3.1 Mutation 算子对比

`swap mutation` 随机选择两个位置并交换。它的优点是扰动很小、实现简单，适合在已有较好解附近做局部微调；缺点是单次移动能力有限，如果某个事件需要跨越较长距离，可能需要很多次 swap 才能完成。

`insert mutation` 随机选择一个事件，并将其插入到另一个位置。它适合排序问题，因为它能直接改变单个事件在全局序列中的位置，同时基本保留其他事件的相对顺序。对于 FAD/LAD 事件排序问题，某个物种事件位置偏差较大时，insert 往往比 swap 更有效。缺点是如果需要同时调整一组事件，单个 insert 仍然偏局部。

`reversal mutation` 随机选择一段连续区间并反转。它的扰动范围比 swap 和 insert 更大，可能帮助跳出局部最优；但在本问题中，连续区间反转容易破坏已有的 FAD/LAD 顺序和局部年代结构，修复后产生的有效变化可能较难控制。

`shuffle mutation` 随机选择若干个事件并打乱。它的探索性强，能快速制造多样性；缺点是方向性较弱，容易破坏已经形成的高质量排序结构。该算子更适合前期探索，不一定适合后期精修。

`shift mutation` 在局部邻域内移动或交换某个事件。它的优点是扰动温和，适合精细调整；缺点是探索范围较小，在搜索陷入平台时可能难以产生足够大的结构变化。

整体来看，`insert` 更符合本问题“事件在时间序列中移动”的结构特征，因此预期会是较强 baseline；`swap` 和 `shift` 更偏局部微调；`reversal` 和 `shuffle` 更偏探索，但可能带来较多破坏性扰动。

### 3.2 Crossover 算子对比

`order crossover` 保留父代 1 的一个连续片段，然后按照父代 2 中的相对顺序补齐剩余事件。它强调继承父代的相对顺序信息，适合排列问题。优点是能较好保留顺序结构；缺点是对具体位置结构的保留相对弱。

`pmx crossover` 保留父代 1 的部分片段，并通过映射关系处理重复元素。它同时保留一段位置结构和父代之间的对应关系。优点是对排列合法性友好，且能继承较明确的片段结构；缺点是映射过程可能引入不直观的位置变化。

`cycle crossover` 根据两个父代之间的元素-位置循环来构造子代。它能较强地保留父代中的绝对位置关系。优点是继承结构清晰；缺点是有时变化幅度偏保守，子代可能与某个父代较为接近，探索能力不足。

对本问题而言，crossover 的有效性取决于父代中是否存在可复用的局部排序片段。如果父代 archive 已经包含若干较高质量解，`pmx` 和 `order` 都可能继承有用片段；如果父代差异较大，过强的位置继承也可能造成冲突，最终仍需要依赖 benchmark 的 repair 过程修复约束。

### 3.3 Mutation 算子对比实验结果

我们首先完成了 5 个 mutation 算子的对比实验。该组实验统一使用：

- `algorithm = map_elites`
- `selection_type = tournament`
- `tournament_size = 3`
- `crossover_type = pmx`
- `correlation_threshold = 0.68`
- `archive_size = 20`
- `pop_size = 20`
- `init_sampler_type = init_v3`
- 不启用 adaptive mutation、动态 correlation threshold、局部搜索、LNS、EDA、threshold accepting 和 diversity bonus。

实验结果保存在：

`results/earth_124/map_elites1/`

对比曲线如下：

![mutation comparison](results/earth_124/map_elites1/comparison_y_best_11.png)

各 mutation 算子的最终结果如下：

| mutation | total evaluations | archive size | final y_best | 排名 |
|---|---:|---:|---:|---:|
| `swap` | 39964 | 20 | -3666 | 1 |
| `insert` | 39964 | 20 | -3678 | 2 |
| `shift` | 39964 | 15 | -3691 | 3 |
| `reversal` | 39964 | 20 | -3735 | 4 |
| `shuffle` | 39964 | 20 | -3943 | 5 |

从最终结果看，`swap` 在本次实验中表现最好，最终达到 `y_best = -3666`，超过原先使用 `insert` 的 baseline `-3678`。`insert` 仍然表现稳定，排名第二。`shift` 的最终值为 `-3691`，略弱于 `insert`，且最终 archive size 只有 15，说明其产生的新解可能更容易与已有 archive 个体相似，导致多样性填充不足。`reversal` 和 `shuffle` 明显较弱，尤其 `shuffle` 最终只有 `-3943`。

从收敛过程看，不同 mutation 的特点也比较明显：

| epoch | swap | insert | shift | reversal | shuffle |
|---:|---:|---:|---:|---:|---:|
| 100 | -4443 | -4322 | -4262 | -4437 | -4667 |
| 250 | -4025 | -3881 | -3804 | -4186 | -4592 |
| 500 | -3827 | -3780 | -3794 | -3918 | -4440 |
| 750 | -3713 | -3699 | -3731 | -3846 | -4216 |
| 1000 | -3690 | -3686 | -3731 | -3795 | -4104 |
| 1500 | -3667 | -3686 | -3691 | -3735 | -4014 |
| 2000 | -3666 | -3678 | -3691 | -3735 | -3943 |

`insert` 和 `shift` 在前期收敛较快，能够较早把解推进到 `-3800` 附近。`swap` 前期略慢，但中后期持续改进，在 `epoch=1191` 左右超过 `-3678`，并最终达到本组实验最好结果。这个现象说明，对 `earth_124` 而言，较小、稳定的局部扰动反而可能更适合后期精修。

`reversal` 和 `shuffle` 的表现较差，主要原因可能是扰动过强。`reversal` 会反转连续区间，容易破坏已有的 FAD/LAD 局部顺序；`shuffle` 会随机打乱多个事件，虽然增加了探索性，但缺乏方向性，修复后仍难以稳定继承父代中的有效排序结构。因此在当前设置下，强探索型 mutation 并没有带来更好的最终结果。

总体来看，本组实验表明：在 `earth_124` 的 MAP-Elites 框架下，小扰动算子更有优势，其中 `swap` 的后期精修能力最好，`insert` 是稳定且较强的 baseline，`shuffle/reversal` 这类强扰动算子不适合作为单一 mutation 长时间使用。

### 3.4 Crossover 算子对比实验结果

随后我们完成了 3 个 crossover 算子的对比实验。该组实验统一使用：

- `algorithm = map_elites`
- `selection_type = tournament`
- `tournament_size = 3`
- `mutation_type = mix`
- `correlation_threshold = 0.68`
- `archive_size = 20`
- `pop_size = 20`
- `init_sampler_type = init_v3`
- 不启用 adaptive mutation、动态 correlation threshold、局部搜索、LNS、EDA、threshold accepting 和 diversity bonus。

实验结果保存在：

`results/earth_124/map_elites1/`

对比曲线如下：

![crossover comparison](results/earth_124/map_elites1/comparison_y_best_12.png)

各 crossover 算子的最终结果如下：

| crossover | total evaluations | archive size | final y_best | 排名 |
|---|---:|---:|---:|---:|
| `cycle` | 39964 | 20 | -3665 | 1 |
| `pmx` | 39964 | 20 | -3681 | 2 |
| `order` | 39964 | 20 | -3695 | 3 |

从最终结果看，`cycle crossover` 表现最好，最终达到 `y_best = -3665`，略优于 mutation 对比实验中的最佳 `swap` 结果 `-3666`。`pmx crossover` 最终达到 `-3681`，略弱于之前固定 `insert + pmx` baseline 的 `-3678`，但仍属于较稳定的结果。`order crossover` 最终为 `-3695`，在三者中最弱。

从收敛过程看，三者差异较明显：

| epoch | cycle | pmx | order |
|---:|---:|---:|---:|
| 100 | -4172 | -4407 | -4984 |
| 250 | -3843 | -3938 | -4785 |
| 500 | -3721 | -3774 | -4345 |
| 750 | -3717 | -3722 | -4037 |
| 1000 | -3692 | -3702 | -3837 |
| 1500 | -3665 | -3702 | -3733 |
| 2000 | -3665 | -3681 | -3695 |

`cycle` 在前期和中期都明显领先，约在 `epoch=1015` 达到 `y_best >= -3678`，并在后期进一步提升到 `-3665`。这说明在 `mutation=mix` 的设置下，保留父代位置循环结构可能更有利于继承有效排序片段，同时保留足够变化。

`pmx` 的表现居中。它能较快进入 `-3800` 到 `-3700` 区间，但后期一度平台化，直到接近实验末期才提升到 `-3681`。这说明 PMX 能稳定继承片段结构，但在该组混合 mutation 设置下，后期突破能力略弱于 cycle。

`order` 的前期表现最慢，直到 `epoch=913` 才达到 `y_best >= -3900`，明显落后于 `cycle` 和 `pmx`。其原因可能是 order crossover 更强调相对顺序继承，而本问题中较好的解不仅依赖相对顺序，也依赖 FAD/LAD 与共生约束修复后的局部位置结构。相对顺序继承过于温和时，配合 `mix` mutation 可能难以快速形成高质量片段。

总体来看，在本次 crossover 对比中，`cycle` 是最优选择，`pmx` 稳定但略弱，`order` 收敛速度和最终值都较差。这也说明在本问题上，保留父代位置结构的 crossover 可能比单纯保留相对顺序更有效。

## 4. Baseline：MAP-Elites + 锦标赛选择

原始 MAP-Elites 的父代选择是从 archive 中随机选择两个解，没有利用已有 fitness。我们首先将父代选择改为锦标赛选择：

1. 从 archive 中随机采样 `tournament_size` 个候选父代。
2. 选择其中 fitness 最高者作为父代。
3. 对第二个父代重复该过程，并尽量避免与第一个父代相同。

该改动只利用 archive 中已有的 `fitnesses`，不会增加任何 benchmark evaluation 次数。实验中的 baseline 使用：

- `selection_type = tournament`
- `tournament_size = 3`
- `mutation_type = insert`
- `crossover_type = pmx`
- `correlation_threshold = 0.68`
- `archive_size = 20`
- `pop_size = 20`
- `init_sampler_type = init_v3`

baseline 完整运行结果位于：

`results/earth_124/map_elites/baseline/`

最终结果：

- `total_evaluations = 39964`
- `y_best = -3678`

## 5. 改进一：adaptive_mix mutation

固定使用 `insert` 虽然稳定，但可能探索不足。因此我们实现了 `adaptive_mix`，在以下变异算子之间动态选择：

- `swap`
- `insert`
- `reversal`
- `shuffle`
- `shift`

选择概率由两部分组成：

1. 阶段性先验：前期偏探索，提高 `shuffle/reversal` 权重；中期以 `insert` 为主；后期偏精修，提高 `insert/swap` 权重。
2. 历史成功率：记录每种 mutation 被尝试的次数，以及其生成的子代是否成功进入或替换 archive。成功率更高的算子后续获得更高权重。

该方法不会增加 evaluation 次数。每个候选解仍然只评估一次，只是在 `tell()` 阶段根据 archive 更新结果统计 mutation 的成功情况。

只加入 `adaptive_mix` 的实验结果位于：

`results/earth_124/map_elites/+adaptive_mix/`

最终结果：

- `total_evaluations = 39964`
- `y_best = -3687`

该结果弱于固定 `insert` baseline。说明在 `earth_124` 上，`insert` 本身已经非常适合该问题；额外加入 `shuffle/reversal` 等探索算子会带来噪声。尽管 adaptive 策略能自动调整概率，但在本次 seed 下并未超过纯 `insert`。

## 6. 改进二：动态 correlation_threshold 与局部搜索

MAP-Elites 的 archive 更新依赖 Kendall correlation。固定 `correlation_threshold` 会同时影响多样性和质量替换。我们实现了动态阈值：

- archive 未完成 warm-up 时，使用原始 `correlation_threshold`，避免 archive bootstrap 被卡住。
- archive 填满到 warm-up size 后，阈值从 `0.9 * correlation_threshold` 逐步增长到 `correlation_threshold`。

最初版本中，动态阈值从 `0.5 * correlation_threshold` 开始，导致前期过严，archive 长时间停在 1 或 2，实际 evaluation 数明显偏少。修正后，archive 能在前几轮正常填满。

我们还加入了轻量局部搜索：

- 每隔 `local_search.freq` 个 epoch 触发。
- 从当前 batch 里选 `top_k` 个高分候选。
- 对每个候选生成少量 `insert/swap` 邻居。
- 评估邻居并回传给 archive。
- 额外 evaluation 被记录在 `local_search_evaluations` 中。

该组合实验结果位于：

`results/earth_124/map_elites/+dynamic_threshold+local_search/`

最终结果：

- `total_evaluations = 40124`
- `local_search_evaluations = 160`
- `y_best = -3686`

该方法没有超过 baseline，反而略差。原因可能是局部搜索会把候选更快推向相似局部结构，而动态阈值虽然改善了 archive 更新节奏，但没有提供足够强的跨 basin 探索能力。

## 7. 改进三：LNS destroy-repair

为了增强跳出局部平台的能力，我们实现了 Large Neighborhood Search 风格的 destroy-repair 算子。该方法不是只移动单个 event，而是一次重构一小组 taxon：

1. 从候选解中随机选择若干个 taxon。
2. 删除这些 taxon 的 FAD 和 LAD 两个 event。
3. 将删除的 FAD/LAD 按随机顺序重新插回序列，并保证 FAD 位于对应 LAD 之前。
4. 生成的新候选仍然交给 `main.py` 的 Ray worker 统一 repair 和 evaluation。

该方法不会在算子内部调用 benchmark，因此不会隐藏增加 evaluation 次数。我们将 LNS 作为低频强扰动算子，并支持停滞后提高触发概率。

实验结果位于：

`results/earth_124/map_elites/+LNS/`

最终结果：

- `total_evaluations = 39964`
- `local_search_evaluations = 0`
- `y_best = -3687`

LNS 在本次实验中没有超过 baseline。其原因可能是当前实现是随机重构版本，并没有在大邻域中进行多候选筛选；它增加了扰动幅度，但扰动方向没有 fitness 引导，因此容易破坏 `insert + pmx` 已经形成的有效局部结构。

## 8. 改进四：threshold accepting 与 diversity bonus

进一步地，我们没有修改 benchmark 的 repair 逻辑，而是在 archive 接收规则上放松前中期约束。这样可以仍然只评价合法 repaired 解，同时允许部分略差但结构不同的候选保留下来。

### 8.1 Threshold accepting

原始规则中，当候选解与 archive 中某个个体过于相似时，只有候选 fitness 不低于该个体时才允许替换。我们加入 threshold accepting：

```text
fitness >= reference_fitness - threshold
```

其中 `threshold` 从 `threshold_accepting_start = 100.0` 线性衰减到 `threshold_accepting_end = 0.0`。前期允许略差解进入 archive，以帮助搜索穿过 fitness valley；后期逐步恢复严格选择。

实现时，为避免覆盖高质量 elite，我们区分两种情况：

- 若候选 fitness 更高，则仍然替换最相似 archive 个体。
- 若候选只是被 threshold 接受，则作为新 archive 成员加入；若 archive 超出容量，则由 archive 原有逻辑删除最差个体。

### 8.2 Diversity bonus

同时，我们给结构差异更大的候选一个逐步衰减的接收加分：

```text
accept_score = fitness + diversity_bonus_weight * (1 - max_correlation)
```

其中 `max_correlation` 是候选与 archive 中最相似个体的 Kendall correlation。`diversity_bonus_weight = 50.0`，并随 epoch 线性衰减到 0。该机制鼓励前期保留更多结构差异，后期回到 fitness 主导。

组合实验结果位于：

`results/earth_124/map_elites/+threshold_accepting+diversity_reward/`

最终结果：

- `total_evaluations = 39964`
- `local_search_evaluations = 0`
- `y_best = -3678`

该组合最终与 baseline 持平，没有突破 plateau。但它在早期更快达到较好的区间：达到 `y_best >= -3690` 的 epoch 为 634，而 baseline 为 970。说明放松 archive 接收门槛确实能提高前期探索效率，但本次实验仍未找到超过 `-3678` 的更优 basin。

## 9. 实验曲线与结果对比

当前主要实验的 `y_best` 对比曲线如下：

![y_best comparison](results/earth_124/map_elites/comparison_y_best_2.png)

实验结果汇总如下：

| 方法 | 目录 | total evaluations | local search evals | final y_best | 备注 |
|---|---|---:|---:|---:|---|
| Baseline | `baseline` | 39964 | 0 | -3678 | `insert + pmx + tournament` |
| adaptive_mix | `+adaptive_mix` | 39964 | 0 | -3687 | 探索算子噪声较大 |
| dynamic threshold + local search | `+dynamic_threshold+local_search` | 40124 | 160 | -3686 | 未突破 baseline |
| LNS destroy-repair | `+LNS` | 39964 | 0 | -3687 | 大扰动但缺少评价引导 |
| threshold accepting + diversity bonus | `+threshold_accepting+diversity_reward` | 39964 | 0 | -3678 | 与 baseline 持平，早期改善 |

## 10. 当前结论与不足

当前最稳定的基础方法原本是 `tournament selection + insert mutation + pmx crossover`。在进一步的算子对比实验中，`swap + pmx` 和 `mix + cycle` 分别取得了 `-3666` 和 `-3665`，说明在固定其他条件后，较小的局部扰动和保留位置结构的 crossover 对 `earth_124` 更有潜力。

`adaptive_mix` 和 LNS 都没有超过 baseline。共同原因是它们增强了探索，但没有足够的问题结构或 fitness 引导；在 `earth_124` 上，随机大扰动容易破坏已有顺序结构。

动态 `correlation_threshold` 和局部搜索没有带来最终提升。局部搜索可能加快局部收敛，但也可能让 archive 更早集中到相似区域，降低后续跳出平台的能力。

threshold accepting 与 diversity bonus 是目前较稳的改动。它没有降低最终结果，且在早期更快达到较优区间，但仍未突破 `-3678` 的 plateau。后续可以进一步调小 threshold 或缩短衰减周期，避免前期过多保留较差候选。

EDA precedence restart 曾作为候选方向实现过，但当前实验效果不理想，因此不作为最终方法采用。一个可能原因是 EDA 从 archive elite 中学习到的 precedence matrix 主要复现当前 basin 的共识顺序，难以提供真正跨 basin 的新结构。

此外，目前主要结论仍基于单个 seed。后续实验应至少运行多个 seed，比较均值、最好值、标准差和收敛曲线，避免将随机波动误判为算法提升。
